// Temporary stub for MoveHandler testing
// Arindam will overwrite this with the actual implementation later.
public class GameMap {
    
    public static boolean isInBounds(int y, int x) {
        // Assume true until map logic is implemented
        return true; 
    }
    
    public static boolean isBlocking(int y, int x) {
        // Assume false (not blocking) until map logic is implemented
        return false;
    }
}